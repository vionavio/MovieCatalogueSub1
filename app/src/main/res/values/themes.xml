<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->

    <style name="Theme.MovieCatalogue" parent="Theme.AppCompat.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/clear_purple</item>
        <item name="colorOnPrimary">@color/dark_gray</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="ViewLine">
        <item name="android:layout_marginStart">@dimen/margin_10dp</item>
        <item name="android:layout_marginTop">@dimen/margin_5dp</item>
        <item name="android:layout_marginEnd">@dimen/margin_10dp</item>
        <item name="android:background">@color/gray</item>
    </style>

    <style name="TextViewDesc">
        <item name="android:layout_marginStart">@dimen/margin_8dp</item>
        <item name="android:layout_marginTop">@dimen/margin_4dp</item>
        <item name="android:layout_marginEnd">@dimen/margin_8dp</item>
    </style>

    <style name="TextViewTitle" >
        <item name="android:layout_marginStart">@dimen/margin_8dp</item>
        <item name="android:layout_marginTop">@dimen/margin_16dp</item>
        <item name="android:layout_marginEnd">@dimen/margin_8dp</item>
        <item name="android:textColor">@color/dark_gray</item>
    </style>
</resources>